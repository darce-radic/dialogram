# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Hocuspocus (Yjs WebSocket server for real-time collaboration)
# Leave empty to use the editor in local-only mode
NEXT_PUBLIC_HOCUSPOCUS_URL=ws://localhost:1234

# Redis (Railway — used by BullMQ webhook queue and Hocuspocus)
REDIS_URL=redis://default:password@localhost:6379

# Webhook signing (HMAC-SHA256 secret for agent webhook payloads)
WEBHOOK_SIGNING_SECRET=your-hmac-secret
